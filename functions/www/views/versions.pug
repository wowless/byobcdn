extends chrome
block content
  p you are looking at versions #{id}
  table
    tr
      td BuildConfig
      td= config.BuildConfig
    tr
      td CDNConfig
      td #[a(href='../c/' + config.CDNConfig)= config.CDNConfig]
