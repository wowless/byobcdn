html
  head
    title tactless.dev
  body
    p this is tactless.dev
    p you are looking at ekey #{id}
    table
      tr
        th archive
        th offset
        th size
      each entry in archiveEntries
        tr
          td #[a(href='../a/' + entry.archive)= entry.archive]
          td= entry.offset
          td= entry.size
